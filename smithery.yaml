ai:
  version: 1
  runtime: typescript
  startCommand:
    type: stdio
    configSchema:
      type: object
      properties:
        apiBase:
          type: string
          default: "https://www.xdhdancer.top/api8888"
          description: "API base URL for the Chinese sensitive word detection service"
      required: []
